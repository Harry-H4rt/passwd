{
  "name": "@passwd/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "passwd mobile app: a Capacitor wrapper around the web vault, so iOS/Android reuse the same crypto and API client",
  "scripts": {
    "sync": "cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6",
    "@capacitor/core": "^6",
    "@capacitor/ios": "^6",
    "@capgo/capacitor-native-biometric": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6"
  }
}
