# site (stub — Phase 6)

Marketing + download site (extension store links, self-host docs, release
binaries). Fully decoupled from the app — recommended: [Astro](https://astro.build)
for a fast static site.

```bash
npm create astro@latest site   # merge into this folder
```
