{
  "name": "@passwd/api-client",
  "version": "0.0.0",
  "description": "Typed client + crypto/vault flows shared by the web vault and the browser extension. Transport only — operates on ciphertext from @passwd/crypto.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --import tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@passwd/crypto": "*"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}
