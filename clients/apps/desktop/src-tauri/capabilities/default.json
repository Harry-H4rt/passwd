{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs plus a native file dialog. No network, no HTTP, no broad filesystem scope — vault I/O is done by the app's own read_vault/write_vault commands on a user-chosen path.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
